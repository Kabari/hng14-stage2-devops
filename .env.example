# Redis auth password – choose a strong random string
REDIS_PASSWORD=change_to_a_strong_password

# Host port the frontend is exposed on
FRONTEND_PORT=3000

# (Optional) Override the internal API URL seen by the frontend container
# API_URL=http://api:8000